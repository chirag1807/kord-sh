# 🧶 Kord
> A lightweight, POSIX-compatible shell implemented in C.

**Kord** acts as the central link between the user and the kernel, 
utilizing system calls for process management, piping, and I/O redirection.